import React from 'react'

const Container = ({children}) => {
  return (
    <div>
      {children} {/*Permite falar exatamente onde o componente será exibido */}
    </div>
  )
}

export default Container
